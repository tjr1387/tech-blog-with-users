{{!-- shows ONE post, as well as associated comments (loop) --}}
  {{!-- no options to alter/delete/create POSTS, BUT options (if logged in) to create/edit/delete comments (but U and D only allowed on comments create dby that user --}}